import groovy.swing.SwingBuilder;

plugins {
    id 'java-library'
    id 'jacoco'
    id 'maven-publish'
    id 'io.freefair.lombok' version '5.3.3.3'
}

description = 'Selenide-Reporter'

group = 'com.github.iSYS-Software'

ext {
    nextVersion = '1.1.6-SNAPSHOT'
}
jar.archiveName="selenide-reporter-" + ext.nextVersion + ".jar"

repositories {
    mavenCentral()
    maven { url 'https://jitpack.io' }
    flatDir {
        dirs 'libs'
    }
}

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(11)
    }
}

tasks.withType(JavaCompile) {
    options.compilerArgs.add("-Xlint:unchecked")
}

dependencies {
    api 'com.codeborne:selenide:6.17.2'
    api 'com.github.realulim:extentreports-java:5.0.18'
    //implementation 'org.freemarker:freemarker:2.3.32'
    //implementation 'com.google.code.gson:gson:2.10.1'
    //implementation 'io.reactivex.rxjava3:rxjava:3.1.6'
    //implementation 'org.reactivestreams:reactive-streams:1.0.4'
    //implementation name: 'extentreports-5.0.12-SNAPSHOT'

    implementation 'junit:junit:4.13'
    implementation 'commons-io:commons-io:2.11.0'
    implementation 'org.apache.commons:commons-lang3:3.12.0'
    implementation 'org.slf4j:slf4j-simple:1.7.32'
    implementation 'org.seleniumhq.selenium:selenium-http-jdk-client:4.9.1'
    implementation 'org.apache.httpcomponents.client5:httpclient5'
    constraints {
        implementation('org.apache.httpcomponents.client5:httpclient5:5.2.1') {
            because 'previous versions have a bug impacting this application'
        }
    }
    implementation 'com.google.code.gson:gson:2.10.1'
}

publishing {
    publications {
        selenideReporter(MavenPublication) {
            from components.java
            versionMapping {
                usage('java-api') {
                    fromResolutionOf('runtimeClasspath')
                }
                usage('java-runtime') {
                    fromResolutionResult()
                }
            }
        }
    }

    repositories {
        maven {
            name = 'Project'
            url = layout.buildDirectory.dir("repo")
        }
        maven {
            name "isysNexus"
            url "https://nexus.isys.de/repository/maven-releases/"
            credentials {
                username 'project.nexusUser'
                password 'project.nexusPassword'
            }
        }
    }
}
